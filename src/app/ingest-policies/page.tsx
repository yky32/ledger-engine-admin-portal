"use client";

import { DoorPanel } from "@/components/rules/door-panel";

/** Legacy URL — content consolidated into /rules (?tab=door). */
export default function IngestPolicyPage() {
  return <DoorPanel />;
}
