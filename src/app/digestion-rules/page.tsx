"use client";

import { BrainPanel } from "@/components/rules/brain-panel";

/** Legacy URL — content consolidated into /rules (?tab=brain). */
export default function DigestionRulesPage() {
  return <BrainPanel />;
}
