"use client";

import { TierPanel } from "@/components/rules/tier-panel";

/** Legacy URL — content consolidated into /rules (?tab=tier). */
export default function WalletTierPoliciesPage() {
  return <TierPanel />;
}
