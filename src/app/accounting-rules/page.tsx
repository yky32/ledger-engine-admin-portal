"use client";

import { AccountingPanel } from "@/components/rules/accounting-panel";

/** Legacy URL — content consolidated into /rules (?tab=accounting). */
export default function AccountingRulesPage() {
  return <AccountingPanel />;
}
