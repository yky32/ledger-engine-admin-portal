"use client";

import { TierPanel } from "@/components/rules/tier-panel";

/** Flow step 5 · Tiering — config surface, consolidated under /rules. */
export default function RulesTierPanelPage() {
  return <TierPanel />;
}
