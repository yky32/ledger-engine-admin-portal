"use client";

import { DoorPanel } from "@/components/rules/door-panel";

/** Flow step 1 · Door — config surface, consolidated under /rules. */
export default function RulesDoorPanelPage() {
  return <DoorPanel />;
}
