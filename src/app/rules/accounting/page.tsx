"use client";

import { AccountingPanel } from "@/components/rules/accounting-panel";

/** Flow step 3 · Accounting — config surface, consolidated under /rules. */
export default function RulesAccountingPanelPage() {
  return <AccountingPanel />;
}
