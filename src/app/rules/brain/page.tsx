"use client";

import { BrainPanel } from "@/components/rules/brain-panel";

/** Flow step 2 · Brain — config surface, consolidated under /rules. */
export default function RulesBrainPanelPage() {
  return <BrainPanel />;
}
